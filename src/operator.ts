const sum: number = 5 + 3;

const difference: number = 8 - 5;

const product: number = 4 * 7;

const quotient: number = 20 / 4;

const remainder: number = 11 % 3;

console.log(`this is result from sum ${sum}`)
console.log(`this is result from difference ${difference}`)
console.log(`this is result from product ${product}`)
console.log(`this is result from quotient ${quotient}`)
console.log(`this is result from remainder ${remainder}`)


// Logika Operator
// AND (&&)
// OR (||)
// NOT (!)

// contoh

// aku harus keluar rumah jika aku ada di rumah, tidak di piyama dan
// mau makan atau bosan

// const shouldGoOutside = isHome && !inPajamas && (wantsToEat || isBored);

const num: number = 5;
const between = num > 5 && num < 7;
const numberIsNotZero = num !== 0;